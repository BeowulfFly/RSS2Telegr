# ============ Telegram Bot ============
BOT_TOKEN=your_bot_token_here

# 目标频道（你自己的频道，Bot 需要是管理员）
TARGET_CHANNEL=@your_channel

# ============ Telegram Client (MTProto) ============
# 从 https://my.telegram.org 获取
TG_API_ID=12345678
TG_API_HASH=your_api_hash_here

# 监控的源频道列表（逗号分隔）
SOURCE_CHANNELS=@channel1,@channel2

# =========================================
# AI 模型配置（支持 OpenAI / DeepSeek 等兼容 API）
# =========================================
# 使用 OpenAI 官方 API 示例：
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o-mini
# 如使用第三方兼容接口可设置 base URL
# OPENAI_BASE_URL=https://api.openai.com/v1

# 使用 DeepSeek API 示例：
# OPENAI_API_KEY=sk-your-deepseek-key
# OPENAI_MODEL=deepseek-chat
# OPENAI_BASE_URL=https://api.deepseek.com

# 启用 AI 聊天功能（非命令消息时使用 AI 回复）
ENABLE_AI_CHAT=true

# ============ Translation ============
# 启用自动翻译功能（默认 true）
ENABLE_TRANSLATION=true
# 翻译阈值：中文字符占比低于此值时翻译为中文（默认 0.2，即 20%）
TRANSLATION_THRESHOLD=0.2

# ============ Filter ============
# 过滤关键词（逗号分隔，匹配到任意一个则保留）
FILTER_KEYWORDS=AI,crypto,tech,blockchain
# 排除关键词（逗号分隔，匹配到任意一个则丢弃）
EXCLUDE_KEYWORDS=广告,spam,推广
# 消息最短长度（字符）
MIN_MESSAGE_LENGTH=20

# ============ Schedule ============
# 抓取间隔 cron 表达式（默认每12小时）
SCRAPE_CRON=0 */12 * * *
# 每日总结 cron 表达式（默认每天22:00）
SUMMARY_CRON=0 22 * * *

# ============ Publisher ============
# 消息发布间隔（毫秒），避免触发 Telegram 频率限制，默认 3000（3秒）
PUBLISH_INTERVAL_MS=3000

# ============ Misc ============
LOG_LEVEL=info
